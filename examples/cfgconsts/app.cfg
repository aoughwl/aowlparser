# the application's configuration, read at COMPILE TIME
appName = "aowlparser demo"
version = "0.4.0"
port = "8080"

# a switch, deliberately not turned into a constant
--path:"$lib"

[logging]
level = "debug"
